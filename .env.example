APP_NAME=feishu-rpa-commerce-agent
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
TZ=Asia/Shanghai
LOG_LEVEL=INFO

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=feishu_rpa
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

MILVUS_HOST=localhost
MILVUS_PORT=19530

FEISHU_APP_ID=cli_a95f7e3bbcfcdbcb
FEISHU_APP_SECRET=96UEASHM1vZLuHznOmAqvcyUogJ52oMf
FEISHU_BOT_NAME=commerce-agent-bot

CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Dev-only mode switch for query_sku_status (mock|api), default mock
PRODUCT_QUERY_SKU_DEFAULT_EXECUTION_MODE=mock

# Dev-only default platform for query_sku_status (mock|woo|odoo|sandbox)
PRODUCT_QUERY_SKU_DEFAULT_PLATFORM=sandbox

# Dev-only sandbox base url for api-mode query_sku_status
PRODUCT_QUERY_SKU_API_BASE_URL=internal://sandbox
PRODUCT_QUERY_SKU_API_TIMEOUT_MS=2000
ENABLE_INTERNAL_SANDBOX_API=true

# Dev credential placeholders for provider auth contract
WOO_API_TOKEN=
WOO_API_KEY=
WOO_API_SECRET=

# Woo read-only production prep config (no production request in current phase)
WOO_BASE_URL=
WOO_API_VERSION_PREFIX=wp-json/wc/v3
WOO_AUTH_MODE=token_header
WOO_READONLY_TIMEOUT_MS=2000
WOO_ALLOW_INTERNAL_SANDBOX_FALLBACK=true
WOO_ENABLE_READONLY_DRY_RUN=false
WOO_ENABLE_READONLY_DRY_RUN_FALLBACK=true
WOO_ENABLE_READONLY_LIVE_PROBE=false
WOO_LIVE_BASE_URL=
WOO_LIVE_TIMEOUT_MS=2000
ODOO_SESSION_ID=
ODOO_DB=