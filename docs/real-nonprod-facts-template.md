# Real Non-Prod Facts Template

> 说明：这是 `real_nonprod_page` 的待确认模板。请只在拿到真实页面 facts 后填写，不要使用 controlled_page 事实替代。

## 必填项

- `entry_url`:
- `session_mode`:
- `navigation_path`:
  - step1:
  - step2:
  - step3:

- `search_area`:
  - `search_input_selector`:
  - `search_button_selector`:
  - `result_row_selector`:

- `editor_area`:
  - `editor_entry_selector`:
  - `editor_container_selector`:

- `submit_area`:
  - `inventory_input_selector`:
  - `submit_button_selector`:

- `feedback_area`:
  - `success_toast_selector`:
  - `error_toast_selector`:
  - `verify_field_selector`:

## 当前阻塞

- real_nonprod_page 的正式 URL 尚未确认
- 登录 / 会话维持方式尚未确认
- 导航入口尚未确认
- 搜索 / 编辑 / 提交 / 回显区稳定定位尚未确认

## 当前结论

- 该模板仅用于收集真实非生产页面 facts
- 不能用 controlled_page 事实填充
- 在 facts 补齐前，P83 正式总演练不能启动
